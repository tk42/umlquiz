version: 1
packages:
  - path: "gen/sqlc"
    name: "sqlc"
    engine: "postgresql"
    schema: "api/0001_create_schema.sql"
    queries: "api/query.sql"

    emit_json_tags: true
    emit_prepared_queries: false
    emit_interface: true
    emit_exact_table_names: false
    emit_empty_slices: true